# LaTeX / Word Report

## BLOCKED

- **PDF**：本机无 `pdflatex` / `xelatex` / `latexmk`，无法编译 `final_paper/main.tex` → `paper.pdf`。
- **Word**：本机无 `pandoc`，无法从 `main.tex` 生成 `final_paper/paper.docx`。

## 已完成

- `final_paper/main.tex`（AAMAS 短文，英文，7 节 + thebibliography，含 `\title`/`\maketitle`/`\cite`/`\bibitem`）。
- 旧论文保留为 `final_paper/main.tex.bak_measurement_governance`。

## 状态

不声明 workflow complete；PDF/Word 需在具备 LaTeX/pandoc 的环境重新生成。
