import { createHash } from "node:crypto";import{existsSync,mkdirSync,readFileSync,writeFileSync}from"node:fs";import{join,resolve}from"node:path";
import{MECHANISM_ARMS,MECHANISM_DISCLOSURE_CONTRACT_REF,type MechanismArmV1}from"./mechanismDisclosureContractV1";import{MECHANISM_FORK_CORE_REF}from"./mechanismForkCoreV1";
export const SEED4_RECOVERY_IDS=Object.freeze([50,51,52,53,55,56,57,58,59,60,61,62,63,64,65]);
export const SEED4_RECOVERY_OUTPUT="experiments/campaign/pilot_output/v6-fork-mechanism-neutral-labeled-seed4-rate-limit-recovery-20260823";const SPEC="docs/experiments/FIRST_PAPER_MECHANISM_SEED4_RATE_LIMIT_RECOVERY_FREEZE_V1.md";
export interface Seed4RecoveryPlanV1{planRef:{id:"swarmalpha.experiment.v6.mechanism-seed4-rate-limit-recovery-plan";version:"1.0.0"};model:"zhipu:glm-4.6v";seed:4;taskIds:number[];arms:MechanismArmV1[];blocks:Array<{taskId:number;seed:4;position:number;expectedAgentCount:number;armOrder:MechanismArmV1[]}>;agentPositions:59;plannedLogicalCalls:413;perCallTokenEstimate:1800;plannedEstimateTokens:743400;tokenStopThreshold:900000;discussionMaxTokens:768;finalMaxTokens:256;thinking:"disabled";concurrency:1;outputDir:string;analysisSpec:{path:string;contentHash:string};disclosureContractRef:{id:string;version:string};forkCoreRef:{id:string;version:string};contentHash:string;}
const sha=(s:string)=>`sha256:${createHash("sha256").update(s).digest("hex")}`;function c(v:unknown):unknown{if(v===null||typeof v!=="object")return v;if(Array.isArray(v))return v.map(c);return Object.fromEntries(Object.keys(v as Record<string,unknown>).sort().map(k=>[k,c((v as Record<string,unknown>)[k])]));}
export function recomputeSeed4RecoveryPlanHashV1(p:Seed4RecoveryPlanV1){const{contentHash:_,...b}=p;return sha(JSON.stringify(c(b)));}
export function buildSeed4RecoveryPlanV1():Seed4RecoveryPlanV1{const orders:MechanismArmV1[][]=[["CONTROL","ATTACKS_NEUTRAL","ATTACKS_LABELED"],["ATTACKS_NEUTRAL","ATTACKS_LABELED","CONTROL"],["ATTACKS_LABELED","CONTROL","ATTACKS_NEUTRAL"]];const original:Record<number,number>={50:29,51:30,52:31,53:32,55:33,56:34,57:35,58:36,59:37,60:38,61:39,62:40,63:41,64:42,65:43};const blocks=SEED4_RECOVERY_IDS.map(taskId=>({taskId,seed:4 as const,position:original[taskId],expectedAgentCount:taskId===50?3:4,armOrder:[...orders[(original[taskId]+1)%3]]}));const body:Omit<Seed4RecoveryPlanV1,"contentHash">={planRef:{id:"swarmalpha.experiment.v6.mechanism-seed4-rate-limit-recovery-plan",version:"1.0.0"},model:"zhipu:glm-4.6v",seed:4,taskIds:[...SEED4_RECOVERY_IDS],arms:[...MECHANISM_ARMS],blocks,agentPositions:59,plannedLogicalCalls:413,perCallTokenEstimate:1800,plannedEstimateTokens:743400,tokenStopThreshold:900000,discussionMaxTokens:768,finalMaxTokens:256,thinking:"disabled",concurrency:1,outputDir:SEED4_RECOVERY_OUTPUT,analysisSpec:{path:SPEC,contentHash:sha(readFileSync(resolve(SPEC),"utf8"))},disclosureContractRef:{...MECHANISM_DISCLOSURE_CONTRACT_REF},forkCoreRef:{...MECHANISM_FORK_CORE_REF}};return{...body,contentHash:sha(JSON.stringify(c(body)))};}
export function freezeSeed4RecoveryPlanV1(dir:string){const p=buildSeed4RecoveryPlanV1(),f=join(resolve(dir),"plan.json");if(existsSync(f)){const e=JSON.parse(readFileSync(f,"utf8"))as Seed4RecoveryPlanV1;if(recomputeSeed4RecoveryPlanHashV1(e)!==e.contentHash||e.contentHash!==p.contentHash)throw new Error("recovery_plan_conflict");return e;}mkdirSync(resolve(dir),{recursive:true});writeFileSync(f,`${JSON.stringify(p,null,2)}\n`);return p;}
export function loadSeed4RecoveryPlanV1(dir:string){const f=join(resolve(dir),"plan.json");if(!existsSync(f))throw new Error("recovery_plan_missing");const p=JSON.parse(readFileSync(f,"utf8"))as Seed4RecoveryPlanV1;if(recomputeSeed4RecoveryPlanHashV1(p)!==p.contentHash||p.contentHash!==buildSeed4RecoveryPlanV1().contentHash)throw new Error("recovery_plan_invalid");return p;}
